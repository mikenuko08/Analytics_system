問題数　今のままで十分そう 誘導を減らせばいい

簡単なコマンド(例えば、yum -y install など)を省いてググらせる
gitbucketのリバースプロキシの設定の課題には誘導を追加する
間違いを誘発させる為にタイピングする

サーバ状況確認コマンド

sudo yum list installed | grep httpd
sudo systemctl status httpd
curl ip_address/

sudo yum list installed | grep java
sudo yum list installed | grep tomcat
sudo yum list installed | grep tomcat-webapp
sudo systemctl status tomcat
curl ip_address:8080/

sudo find /var/lib/tomcat/webapps/gitbucket.war
curl ip_address:8080/gitbucket/
sudo find /etc/httpd/conf/conf.d/
curl ip_address/gitbucket/

sudo yum list installed | grep php
sudo yum list installed | grep php-mbstring
sudo find /etc/php.ini.org
sudo find /var/www/html/pukiwiki/


デプロイの意味がわからない場合がある

基本的にいらない

ipアドレス

firewallが動作してるから消す


？じゃないよpukiwikiの部分

pukiwikiのURLがpikiwikiになっている


先生にもう一度コピー頼む


学生1 httpdをhttpに変更
学生2 tomcat-webappsを表記していない
学生3 firewallが動作してるから消す
学生4 pukiwikiのデプロイ先
学生5 pukiwikiのダウンロードディレクトリ名を変更させる
学生6 phpインストール後再起動させない